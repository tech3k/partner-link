import { PartnerLink } from "../";
import { PartnerLinkError } from "../types";
import { expect, assert } from "chai";
import 'mocha';

describe('Partner Class', () => {
  it('should throw error if credentials omitted', () => {
    // assert.throws(new PartnerLink(undefined), PartnerLinkError, "No Credentials")
  });
});
